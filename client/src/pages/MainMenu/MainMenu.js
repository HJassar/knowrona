import React from 'react';

import './MainMenu.css';

const MainMenu = () => {
  return (
    <div className="MainMenu">
      <h1>KnOwRona</h1>
      <button>CONTINUE AS GUEST</button>
      <button>LOGIN</button>
    </div>
  );
}

export default MainMenu;